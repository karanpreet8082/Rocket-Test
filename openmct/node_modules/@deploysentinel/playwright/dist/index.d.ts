import { Reporter, FullConfig, Suite, TestCase, TestResult, FullResult } from '@playwright/test/reporter';

declare class DeploySentinelReporter implements Reporter {
    private readonly enabled;
    private readonly ciProvider;
    private readonly apiClient;
    private readonly gitClient;
    private readonly logger;
    private readonly runsPromise;
    private readonly promises;
    private readonly isIpc;
    constructor();
    private wrap;
    private wrapAsync;
    printsToStdio(): boolean;
    onBegin(config: FullConfig, _: Suite): void;
    onTestBegin(test: TestCase, result: TestResult): void;
    onTestEnd(test: TestCase, result: TestResult): void;
    onEnd(_: FullResult): Promise<void>;
}

export { DeploySentinelReporter as default };
